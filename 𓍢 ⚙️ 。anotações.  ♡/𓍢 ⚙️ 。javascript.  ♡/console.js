//  Metódos do 'console':
    console.log();      // imprime na tela.
    console.clear();    // limpa o console.
    console.warn();     // warn:

/*  A verificação do tipo de uma variável (se ela é boolean, number, string, symbol ou object) é realizada
    através da inserção do 'typeof' nos parênteses do console:
*/
    console.log(typeof nomeVariavel);
